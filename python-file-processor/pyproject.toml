[tool.poetry]
name = "python-file-processor"
version = "0.1.0"
description = "A simple Python project that reads an input file, processes its contents, and writes a final output file."
authors = ["Generated by GitHub Copilot"]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]
pytest = "^7.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
